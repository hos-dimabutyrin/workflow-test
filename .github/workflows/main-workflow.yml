name: Main Workflow

on:
  push:
    branches:
      - dev
      - staging
      - main

jobs:
  test:
    name: "Run Main Workflow"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Run tests
        run: |
            echo "OK"
